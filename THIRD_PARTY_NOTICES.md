# Third-party notices

The builder is derived from this project's Apache-2.0 research implementation.
This release modularizes and modifies that implementation. The license is
provided in LICENSE.

The OpenAI Python SDK, MCP Python SDK, tqdm, and optional spaCy installation remain separate
dependencies under their respective licenses; their source code is not vendored.
No model weights, benchmark datasets, human annotations, or retrieved web
excerpts are distributed. The example trajectory is fictional and uses the
reserved example.org domain.

Codex CLI and Claude Code are optional, separately installed tools governed by
their providers' licenses and service terms. SearchAtlas does not bundle them.
